import { ReactElement } from 'react';
import './ListIcon.scss';

/**
 * Houses a little list icon.
 * @returns a ReactElement
 */
export const ListIcon = (): ReactElement => (
  <>
    <div className="list-icon"></div>
  </>
);
