import { ReactElement } from 'react';
import './GridIcon.scss';

/**
 * Houses a little grid icon.
 * @returns a ReactElement
 */
export const GridIcon = (): ReactElement => (
  <>
    <div className="grid-icon"></div>
  </>
);
