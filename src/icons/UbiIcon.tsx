import { ReactElement } from 'react';
import './UbiIcon.scss';

/**
 * Houses the Ubiquiti icon. Size and shape are defined in parent elements.
 * @returns a ReactElement
 */
export const UbiIcon = (): ReactElement => (
  <>
    <div className="ubi-icon"></div>
  </>
);
